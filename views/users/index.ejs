<% layout('layouts/boilerplate') %>

<div class="container px-4 mx-width-profile mx-auto mt-3">
  <div class="d-flex gap-4">
    <img
      src="https://d33wubrfki0l68.cloudfront.net/fb0ba0cbcadbdf0b2ffabf331d2345fb05106e16/20663/images/profile-picture.webp"
      alt=""
      class="rounded-circle profile-image-index"
    />
    <div class="mt-1">
      <h2 class="fw-normal fs-3"><%= user.username %></h2>
      <% if(currentUser && !user._id.equals(currentUser._id)){ %>
      <form
        action="/follow/<%= user._id %>/<% if (!(currentUser && currentUser.following.includes(user._id))){ %>follow<% } else { %>unfollow<% } %>"
        method="POST"
        class="follow-form"
      >
        <% if (!(currentUser && currentUser.following.includes(user._id))){ %>
        <button class="btn btn-primary btn-sm px-3 btn-follow">Follow</button>
        <% } else { %>
        <button class="btn btn-outline-secondary btn-sm px-4 btn-unfollow">
          <span class="fs-7 fw-5">Unfollow</span>
        </button>
        <% } %>
      </form>
      <% } else if (!currentUser){%>
      <a href="/login">
        <button class="btn btn-primary btn-sm px-3 btn-follow">Follow</button>
      </a>
      <% }else {%>
      <a href="/accounts/edit"
        ><button
          class="btn btn-outline-secondary btn-sm px-2 btn-unfollow fw-5"
        >
          Edit profile
        </button></a
      >
      <% } %>
    </div>
  </div>
  <div class="mt-3 d-flex flex-column">
    <span class="fs-6 fw-5"><%= user.fullName %> </span>
    <span class="bio-text fs-6"><%= user.bio %></span>
  </div>
</div>

<!-- <hr class="mb-2 text-muted" /> -->

<div
  class="py-2 mt-3 mx-width-profile mx-auto border-top border-bottom border-end border-start"
>
  <div class="d-flex justify-content-around my-0 text-center fs-7">
    <div>
      <div class="fw-bold"><%= user.posts.length %></div>
      <div class="text-muted">posts</div>
    </div>
    <div>
      <a href="<%= user.username %>/followers" class="text-reset">
        <div class="fw-bold" id="followers-length">
          <%= user.followers.length %>
        </div>
        <div class="text-muted">followers</div>
      </a>
    </div>
    <div>
      <a href="<%= user.username %>/following" class="text-reset">
        <div class="fw-bold"><%= user.following.length %></div>
        <div class="text-muted">following</div>
      </a>
    </div>
  </div>
</div>

<!-- <hr class="mt-2 mb-0 text-muted" /> -->

<!-- <div class="container">
  <div class="row">
    <% for (let post of user.posts){ %>
    <div class="m">
      <div class="profile-post-img">
        <img class="img img-fluid" src="<%= post.image %>" alt="" />
      </div>
    </div>
    <% } %>
  </div>
</div> -->

<div
  class="text-center py-3 gap-2 d-flex justify-content-center align-items-center"
>
  <i class="fa-solid fa-table-cells-large fs-7 text-muted"></i>
  <span class="text-muted fs-7 fw-bold">POSTS</span>
</div>

<section class="post-list">
  <% for (let post of user.posts){ %>
  <a href="/p/<%= post._id %> " class="post">
    <figure class="post-image profile-post-img">
      <img class="img img-fluid" src="<%= post.image %>" alt="" />
    </figure>
    <span class="post-overlay">
      <p>
        <span class="post-likes"
          ><i class="fa-solid fa-heart fs-6 me-1"></i> <%= post.likes.length %>
        </span>
        <span class="post-comments"
          ><i class="fa-solid fa-comment fs-6 me-1"></i> <%=
          post.comments.length %>
        </span>
      </p>
    </span>
  </a>
  <% } %>
</section>
